type NullableBoolean = boolean | null;

type Boolean = boolean;

type BuildingData = {
    r_no:string,
    f_name:string,
    des:string,
    flr:string,
    ins:string
}

export{NullableBoolean,Boolean,BuildingData};